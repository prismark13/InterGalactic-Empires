# InterGalactic-Empires Environment Configuration
# Copy this file to .env and fill in your actual values (do not commit .env)

# Google Generative AI API Key (required for AI features, optional for local dev)
# Get a key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: override the model name (defaults to gemini-2.0-flash)
# MODEL=gemini-2.0-flash

# Optional: override the server port (defaults to 3000)
# PORT=3000
